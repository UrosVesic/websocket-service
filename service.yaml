apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  ports:
    - port: 8084
      protocol: TCP
      targetPort: 8084
  selector:
    app: auth-service
  sessionAffinity: None
  type: LoadBalancer